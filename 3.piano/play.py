import winsound

winsound.Beep(500, 1000)  # 前一个参数是频率，后一个参数是持续时间

winsound.Beep(500, 1000)  # 前一个参数是频率，后一个参数是持续时间

winsound.Beep(600, 1000)  # 前一个参数是频率，后一个参数是持续时间

winsound.Beep(600, 1000)  # 前一个参数是频率，后一个参数是持续时间

## 废弃，调用这个库会出问题
